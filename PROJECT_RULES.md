# 项目开发规范 (PROJECT_RULES)

本文档定义本项目的开发流程、规范与协作原则。

---

## 1. 核心开发工作流

### 方案设计与审批制度（硬性约束）
1. **方案设计**：任何涉及代码修改、功能迭代或架构变更的操作，必须先撰写并向用户展示技术方案（包括改动点、影响文件、风险防范）。
2. **审批确认**：方案必须等待用户审查通过（显式授权同意）后，方可启动代码编写。
3. **测试落地**：代码修改完成后，必须运行指令进行Empirical验证，确认无误后向用户总结汇报。

---

## 2. 架构与运行规范

- **单进程守护**：多博主定时增量抓取统一由 Supervisor 托管的单进程守护服务 (`dy_monitor_daemon.ts`) 维护，严禁开启多进程并发扯皮。
- **串行平滑**：抓取任务严格串行执行，博主间维持安全休眠间隔。
- **结构化导出**：每个作品保存为独立目录，包含高清视频/图集、封面、音频及 `info.json`。
