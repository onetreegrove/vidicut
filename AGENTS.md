# Agent Working Guidelines & Project Rules

## 🚨 核心开发工作流规范 (Mandatory Workflow Rules)

### 1. 方案先行，审批后落地 (Design First & User Approval Required)
- **修改前先设计**：在进行任何代码修改、功能新增、重构或架构调整前，必须先在对话中向用户呈现完整清晰的技术方案设计（包含：需求理解、设计思路、拟修改的文件清单、逻辑变动及风险评估）。
- **等待用户审批**：方案输出后，必须明确等待用户确认/审批通过（例如用户回复“同意”、“按此方案”或给出明确认可），在获得显式授权前，**严禁擅自调用工具修改任何代码或配置文件**。
- **按图施工与验证**：审批通过后，严格按照已批准的方案执行代码修改，并在完成后提供明确的运行测试与验证汇报。

---

## 📂 代码与项目规范 (Project Architecture Standards)

### 1. 抖音下载资源目录规范
所有抖音解析与抓取的媒体素材，必须统一按照以下二级分类规则归档落盘：
- **带有合集的作品**：`./downloads/{博主名}/{合集名}/{作品ID_作品标题}/`
- **不属于合集的作品**：`./downloads/{博主名}/单视频/{作品ID_作品标题}/`

### 2. 凭证与安全规范
- **环境变量存储**：`ttwid`、`sessionid` 等凭证信息必须统一管理在根目录 `.env` 文件中，严禁硬编码于源代码。
- **Git 忽略敏感文件**：`.env` 与下载目录 `./downloads` 保持在 `.gitignore` 中。

### 3. 防风控与串行原则
- 批量/全量抓取必须采用**严格串行队列**。
- 请求与素材下载之间必须保持随机打散休眠（Jitter Delay），严禁高并发陡峭拉取。
